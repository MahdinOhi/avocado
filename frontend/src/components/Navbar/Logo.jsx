import React from "react";

export default function Logo() {
    const style = {
        fontWeight: "bold",
        fontSize: "1.125rem",
        cursor: "pointer",
    };

    return (
        <div style={style}>
            Avocado Inc.
        </div>
    );
}
